apiVersion: v1
kind: Service
metadata:
  name: tuppr-dashboard
  namespace: system-upgrade
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app.kubernetes.io/name: tuppr-dashboard
    app.kubernetes.io/instance: tuppr-dashboard
